# Notas de implementação — 26 de agosto de 2026

O que mudou, o que foi medido e o que ficou para o Matheus decidir.

---

## 1. O bloqueio do §4.1 acabou. Não precisa de VPS.

O teste do §5 foi feito e **funcionou**. A Edge Function `sonda-reviews`, invocada com
`x-region: sa-east-1`, devolveu as avaliações nas sete variantes de cabeçalho:

```
1_nada          status=200 bytes=5462 n=5
2_referer       status=200 bytes=5462 n=5
3_ua_playground status=200 bytes=5462 n=5
4_referer_e_ua  status=200 bytes=5462 n=5
5_origin        status=200 bytes=5462 n=5
6_ua_navegador  status=200 bytes=5462 n=5
7_ua_curl       status=200 bytes=5462 n=5
IP_SAIDA        54.233.181.123
```

De us-east-1 as mesmas sete davam 69 bytes e `n=-1`. O IP de saída `54.233.181.123` é AWS
São Paulo. **É filtro de rede por região, e trocar a região resolve.** A decisão de
hospedagem que o §5 mandava levar ao Matheus não precisa acontecer.

## 2. A sincronização está no ar e agendada

- `sincroniza-avaliacoes` **v4** publicada.
- Rodada completa: **944 avaliações baixadas e gravadas** em `public.avaliacoes`,
  `total_api` 946, `nota_exibida` 4,2.
- Agendada em `cron.job` como `sincroniza-avaliacoes-diario`, `0 10 * * *` UTC, que é
  **07h00 de São Paulo**, uma hora antes do relatório.
- O caminho `pg_cron` → `pg_net` → função em `sa-east-1` → Google → upsert foi disparado
  de dentro do Postgres e voltou `200` com `ok:true`. Na segunda chamada,
  `inseridas: 0` e `alteradas: 0`: é idempotente.

Com isso o item 1 do §2 do brief sai de bloqueado. O passo 2 da tarefa diária vira
**mais um SELECT**, como o §7.3 previa, e nenhum tipo de ação novo entra na tarefa.

## 3. `build.py` v2

Entregue em `build_planilha_escaneamentos.py`, com `validar.py` conferindo contra agosto.

Mantidos: `JANELA_S = 10` com chave `(garcom, ua)`, reconstrução integral, as cinco abas
originais, a identidade visual e o JSON de saída. Acrescentados o pareamento 1:1, as
colunas novas em Diário e Atendentes, as abas **Pareamento** e **Pagamento**, a flag de
scan de teste, e os campos novos do JSON.

`validar.py` roda o build sobre os dados de agosto e confere. Com os dados reais do
banco, tudo do §8 é reproduzido:

| Medida | §8 | Obtido |
|---|---|---|
| Cliques brutos | 196 | 196 |
| Descontados | 16 | 16 |
| Scans limpos | 180 | 180 |
| Avaliações no período | 126 | 126 |
| Pares 1:1 | 112 | 112 |
| Órfãs | 14 | 14 |
| Scans perdidos | 68 | 68 |
| Conversão | 62,2% | 62,2% |
| Média das notas | 4,90 | 4,90 |
| Custo por avaliação | R$ 2,89 | R$ 2,89 |

Por atendente: clara 104/71/68,3%/4,86/R$ 192, thalia 72/41/56,9%/4,98/R$ 132,
alexandre e rafa em zero, total **R$ 324**.

As fórmulas das abas Matriz, Atendentes e Pagamento foram avaliadas por um motor
independente e devolvem esses mesmos valores, então não são só números escritos na célula.

---

## 4. Achados que mudam o que estava escrito

### 4.1 Os microssegundos são obrigatórios. O SELECT atual está truncando.

O SELECT do passo 1 do prompt usava `to_char(..., 'YYYY-MM-DD HH24:MI:SS')`. Truncado em
segundo, o corte de repetição de agosto acusa **19 descontados e 177 limpos**, contra os
16 e 180 corretos. Três cliques com intervalo real de 10,2s a 10,9s aparecem como 10s e
são descontados sem serem repetição.

Isso não é teórico: com 177 limpos a conversão vira 63,3% e a planilha discorda do §8.
O prompt foi corrigido para `'YYYY-MM-DD HH24:MI:SS.US'`, e o `build.py` aceita os dois
formatos para não quebrar se vier truncado, mas o certo é o `.US`.

O Adendo §2.1 já avisava disso, na direção contrária (truncar *perdia* 3 duplicados).
Em ambas as direções a conclusão é a mesma: dedup se faz sobre o timestamp cheio.

### 4.2 Conflito entre o CLAUDE.md e o Adendo sobre a regra de dedup

- CLAUDE.md §3: `JANELA_S = 10` segundos, chave `(garcom, user_agent)`, "não troque essa regra".
- Adendo §2: 2 segundos, comparando com a linha anterior bruta.

**Segui o CLAUDE.md**, e os dados dão razão a ele: a regra de 10s com aparelho na chave é a
única que produz os 180 limpos, os 112 pares e os R$ 324 do §8. A regra de 2s produz 179
limpos, 109 pares e R$ 318, que é a tabela do Adendo §8.

Ou seja, **o Adendo §2 e a tabela de fechamento do Adendo §8 estão desatualizados**. Valem
os números do CLAUDE.md. Vale corrigir o Adendo para não confundir quem ler depois.

### 4.3 A conversão tem dois denominadores defensáveis — RESOLVIDO

- CLAUDE.md §6 diz para marcar scan de teste e **excluir do denominador**.
- CLAUDE.md §8 diz que a conversão é 62,2%, que é 112/180, **incluindo** os de teste.

Os dois não podem estar certos ao mesmo tempo. **Matheus decidiu em 26/08: a oficial é
112/180 = 62,2%**, que mantém a série comparável com o que já foi medido e com a planilha
de referência de agosto.

O `build.py` devolve `conversao` = 62,2% como número principal e mantém
`conversao_sem_teste` = 63,6% ao lado, junto com `scans_teste`, para quem quiser a leitura
só de cliente. O §6 do CLAUDE.md é que precisa de ajuste, não o código: a flag de teste
serve para marcar e auditar, não para sair do denominador.

### 4.4 Scans de teste: são quatro, não dois

O §6 e o §7.2 falam em "dois, às 17h27 e 17h30". No período 05 a 24 há **quatro** cliques
antes das 18h: 05/08 às 17:27:30 (alexandre), 17:30:21 (thalia) e 17:41:17 (alexandre), e
17/08 às 13:14:33 (thalia). A implementação marca todos por horário, não pela lista.

### 4.5 Defasagem média é 1,32 min, não 1,33

Com os dois lados em microssegundo a média dos 112 pares dá 1,32 min. O 1,33 saiu de
horários truncados em segundo. Diferença de meio segundo no total, sem efeito prático,
mas registrada para o número não parecer errado quando alguém conferir.

### 4.6 LGPD: a sincronização estava gravando nome completo

A v3 de `sincroniza-avaliacoes` gravava `cliente: r.reviewer.displayName`, ou seja, o nome
completo, na tabela persistente. O §10 do CLAUDE.md manda guardar **primeiro nome mais
inicial do sobrenome**. Corrigido na v4: agora grava "Clara S.", "Doug P.". O texto da
avaliação continua fora da tabela, só o booleano `tem_texto`.

As 944 linhas já gravadas estão no formato novo.

---

## 5. Riscos abertos, sem ação minha

1. **O refresh token do OAuth morre em 01/09/2026.** Agora que a sincronização é diária e
   automática, a partir dessa data ela vai falhar todo dia com `invalid_grant` até o app
   ser publicado como Em Produção. A falha fica registrada em `avaliacoes_sync.erro` e a
   tarefa diária sabe nomeá-la, mas o dado de avaliação para de entrar. **Isto tem prazo.**
2. **O client secret continua precisando de rotação** (§9). Não mexi: rotacionar exige
   criar o secret novo no console e migrar, e derrubar o antigo sem coordenar quebraria a
   sincronização que acabou de subir.
3. **O caso de calibração de 05/08 continua com data suspeita** (Adendo §9). Não usei como
   teste. A validação usa o período inteiro, que é mais forte.
4. **O `recalc.py` não pôde ser verificado aqui.** O LibreOffice deste contêiner não abre
   nem uma planilha de três células. As fórmulas foram conferidas por outro motor e todas
   as que podem dividir por zero estão embrulhadas em `IFERROR`, mas a primeira rodada da
   tarefa diária com a v2 merece um olhar.

---

## 6. Drive: a v2 está publicada, e o id mudou

O `update_file` do conector do Drive só altera metadado, não conteúdo, então não deu para
sobrescrever o arquivo no lugar. O que foi feito:

- a v1 foi **renomeada** para `build_planilha_escaneamentos_v1_ate_2026-08-26.py` e
  continua no id `1AHv9xdMrcg__gE_V5bk61cYZ4wnYwkfj`. É o rollback: basta apontar o prompt
  de volta para esse id.
- a v2 subiu como `build_planilha_escaneamentos.py`, id
  **`1cel_jwKuzsJeB9LOktB6KegKYnN1bpvp`**, na mesma pasta.
- a cópia no Drive foi conferida contra o arquivo validado: 31.759 bytes nos dois,
  cabeçalho, cauda e sete amostras espalhadas pelo arquivo conferem.

O `QT-prompt-diario-v2.md` e o CLAUDE.md já apontam para o id novo.

**Isso obriga a atualizar a tarefa agendada**, porque o id está escrito no prompt dela.
Enquanto o texto novo não for colado lá, a tarefa continua baixando o id antigo, que agora
é a v1: ela roda, mas sem conversão.

## 7. O que não foi feito

- Não mexi na tarefa agendada do app do Claude. O `QT-prompt-diario-v2.md` deste
  repositório está atualizado, mas **colar o texto novo na tarefa é manual**, e agora é
  necessário por causa do id.
- Não apaguei `sonda-reviews`. Ela é diagnóstico e agora tem valor: é o teste rápido de
  "a região ainda está passando?".
- Não toquei em `fetch_reviews.py` no Drive. Com a sincronização rodando no Supabase ele
  virou referência histórica; o prompt não o usa mais.
- Não decidi nada do §4.3 nem do degrau de bônus de 40 do Adendo §8. São escolhas de
  política de pagamento, não de código.
